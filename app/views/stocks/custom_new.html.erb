<% provide(:title, "Adding stock to cupboards") %>

<%= react_component("NewStock", {
	cupboardId: @cupboard_id,
	cupboardName: @cupboard_name,
	ingredients: @ingredients,
	units: @units,
	csrfToken: form_authenticity_token
}) %>
