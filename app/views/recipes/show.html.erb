<% provide(:title, "Recipe: #{@recipe.title}") %>
<div class="main recipe-show">
  <div class="container">
    <div class="recipe">
      
      <!-- Display the recipe's info here -->
      <div class="info">
        <h3 class="recipe-title"><%= @recipe.title %></h3>
        <p class="recipe-description"><%= recipe_description %></p>
        <% if current_user && current_user.admin %>
          <%= link_to "Edit recipe", edit_recipe_path(@recipe) %>
        <% end %>
      </div>
    </div>

    <% if current_user %>
      <% if not current_user.favourites.include?(@recipe) %>
        <%= link_to "favourite", favourite_recipe_path(@recipe, type: "favourite"), method: :put %>
      <% else %>
        <%= link_to "unfavourite", favourite_recipe_path(@recipe, type: "unfavourite"), method: :put %>
      <% end %>
    <% end %>
    <br />
    <br />
    <p><% if vegetarian?(@recipe) %>Vegetarian &check;<% end %></p>
    <br />
    <br />


    <% @portions.each do |portion| %>
    <div class="portion-block">

      <p><%= portion_amount(portion) %><%= portion_unit_and_ingredient(portion) %></p>
      <br />
    </div>
    <% end %>






  </div>
</div>
