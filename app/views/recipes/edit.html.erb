<% provide(:title, "Editing Recipe: #{@recipe.title}") %>


<%= form_for @recipe, :html => {class: 'content_rich--wrapper', id: 'recipe_edit'} do |f| %>
	<%= render 'recipe_form_partial', f: f %>
<% end %>
