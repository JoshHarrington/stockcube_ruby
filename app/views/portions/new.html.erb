<%= react_component("NewPortion", {
	recipeId: params.has_key?(:recipe_id) ? params[:recipe_id] : nil,
	recipeName: params.has_key?(:recipe_id) ? Recipe.find(params[:recipe_id]).title : nil,
	ingredients: @ingredients,
	units: @units,
	csrfToken: form_authenticity_token
}) %>
