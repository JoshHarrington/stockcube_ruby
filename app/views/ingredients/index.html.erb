<div class="main ingredient-index">
      <div class="container">
        <div class="row">

          <% @ingredients.sort_by {|ingredient| ingredient.recipes.length}.reverse.each do |ingredient| %>
          
          <div class="ingredient col-xs-2">
            <h3><%= ingredient.name %></h3>
            <h4><%= ingredient.recipes.length %></h4>

            <%= link_to "Learn more", ingredient_path(ingredient) %>
          </div>
          <% end  %>
					<%= link_to 'New Ingredient', '/ingredients/new' %>
          
      </div>
</div>
